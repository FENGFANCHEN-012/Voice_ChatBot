[project]
name = "voice-rag-assistant"
version = "0.1.0"
description = "Browser-based Voice RAG Assistant"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
